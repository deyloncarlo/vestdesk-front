import { FilterFormDirective } from './filter-form.directive';

describe('FilterFormDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterFormDirective();
    expect(directive).toBeTruthy();
  });
});
